# sentinel-packager

Auto-package folder in S3 when sentinel file appears

## Usage

```bash
npx projen
npx projen install
npx projen build
npx projen deploy
```

## Development

```bash
npx projen test
```
